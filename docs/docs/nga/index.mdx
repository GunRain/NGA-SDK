---
sidebar_position: 2
---

import DocCardList from '@theme/DocCardList'

# NGA SDK

NGA SDK 目前支持如下语言:

- C++ (C++ `20`+)
- Dart (仅 Flutter 可用，Dart `2.12`+)
- Go (Go `1.18`+)
- Shell (基于 `POSIX`/`ksh`)

### 通过下列卡片前往各个内容

<DocCardList />
